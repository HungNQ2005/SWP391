<%-- 
    Document   : managecdkey
    Created on : Oct 1, 2025, 4:21:06 PM
    Author     : Nguyen Quoc Hung - CE190870
--%>

<%@page contentType="text/html" pageEncoding="UTF-8"%>
<%@taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c"%>
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>CD Key Management</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 30px; }
        table { width: 100%; border-collapse: collapse; margin-top: 12px; }
        th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
        tr:nth-child(even){ background-color: #f9f9f9; }
        .pagination { margin: 8px 0; }
        .pagination a { margin-right: 6px; text-decoration: none; color: #007bff; }
        .pagination b { margin-right: 6px; color: #333; }
        .tabs {
            margin-top: 20px;
            border-bottom: 2px solid #ccc;
            display: flex;
        }
        .tab {
            padding: 10px 20px;
            cursor: pointer;
            border: 1px solid #ccc;
            border-bottom: none;
            background: #f2f2f2;
            margin-right: 5px;
            border-top-left-radius: 8px;
            border-top-right-radius: 8px;
        }
        .tab.active {
            background: white;
            font-weight: bold;
        }
        .tab-content {
            display: none;
        }
        .tab-content.active {
            display: block;
            background: white;
            border: 1px solid #ccc;
            border-radius: 8px;
            padding: 15px;
        }
    </style>
</head>
<body>

<h2>CD Key Management</h2>

<!-- Create form -->
<form action="${pageContext.request.contextPath}/admin/cdkey" method="post">
    <label>Số lượng Key:</label>
    <input type="number" name="count" min="1" required />
    <button type="submit">Generate</button>
</form>

<div class="tabs">
    <div class="tab active" onclick="showTab('unused')">Unused Keys</div>
    <div class="tab" onclick="showTab('used')">Used Keys</div>
</div>

<!-- UNUSED KEYS TAB -->
<div id="tab-unused" class="tab-content active">
    <h3>Unused CD Keys</h3>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Code</th>
                <th>Status</th>
                <th>Generated By</th>
                <th>Generated Time</th>
            </tr>
        </thead>
        <tbody>
            <c:if test="${empty unusedList}">
                <tr><td colspan="5">No unused keys.</td></tr>
            </c:if>
            <c:forEach var="k" items="${unusedList}">
                <tr>
                    <td>${k.keyID}</td>
                    <td>${k.keyCode}</td>
                    <td>${k.keyStatus}</td>
                    <td>${k.keyGeneratedBy}</td>
                    <td>${k.keyGeneratedTime}</td>
                </tr>
            </c:forEach>
        </tbody>
    </table>

    <div class="pagination">
        <c:forEach var="i" begin="1" end="${totalUnusedPages}">
            <c:choose>
                <c:when test="${i == unusedPage}">
                    <b>${i}</b>
                </c:when>
                <c:otherwise>
                    <a href="${pageContext.request.contextPath}/admin/cdkey?unusedPage=${i}&usedPage=${usedPage}">${i}</a>
                </c:otherwise>
            </c:choose>
        </c:forEach>
    </div>
</div>

<!-- USED KEYS TAB -->
<div id="tab-used" class="tab-content">
    <h3>Used CD Keys</h3>
    <table>
        <thead>
            <tr>
                <th>Key ID</th>
                <th>Code</th>
                <th>Used By (username)</th>
                <th>Used Full Name</th>
                <th>Used At</th>
            </tr>
        </thead>
        <tbody>
            <c:if test="${empty usedList}">
                <tr><td colspan="5">No used keys.</td></tr>
            </c:if>
            <c:forEach var="r" items="${usedList}">
                <tr>
                    <td>${r.keyID}</td>
                    <td>${r.keyCode}</td>
                    <td>${r.usedBy}</td>
                    <td>${r.usedFullName}</td>
                    <td>${r.usedAt}</td>
                </tr>
            </c:forEach>
        </tbody>
    </table>

    <div class="pagination">
        <c:forEach var="i" begin="1" end="${totalUsedPages}">
            <c:choose>
                <c:when test="${i == usedPage}">
                    <b>${i}</b>
                </c:when>
                <c:otherwise>
                    <a href="${pageContext.request.contextPath}/admin/cdkey?usedPage=${i}&unusedPage=${unusedPage}">${i}</a>
                </c:otherwise>
            </c:choose>
        </c:forEach>
    </div>
</div>

<script>
    function showTab(tabName) {
        // Hide all
        document.querySelectorAll('.tab-content').forEach(el => el.classList.remove('active'));
        document.querySelectorAll('.tab').forEach(el => el.classList.remove('active'));

        // Show selected
        document.getElementById('tab-' + tabName).classList.add('active');
        event.target.classList.add('active');
    }
</script>

</body>
</html>